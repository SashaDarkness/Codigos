package cursos.curso01.desafios_feitos_na_aula;

import javax.swing.*;

public class desafio04 {
    public static void main (String[] args) {
    //CONTADOR
    int i = 0, cont1 = 0, cont2 = 0;
    //VALOR DO SENO DE X
    double senX = 0;

    int valor_x = Integer.parseInt(JOptionPane.showInputDialog(null,"Informe o valor: "));

    while (valor_x != 0) {
       // double resultado_positivo = (Math.pow(x, cont1) / (cont1) );
      //  double resultado_negativo = -((Math.pow(x, cont2)) / (cont2) );
        cont1 += 4;
        cont2 += 3;

    }

    }
}
